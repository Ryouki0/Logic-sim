import { MINIMAL_BLOCKSIZE } from "./defaultDimensions";

export const textStlye:React.CSSProperties = {
	color: 'white',
	fontSize: MINIMAL_BLOCKSIZE/2 +4,
	marginTop: 10,
	borderStyle: 'solid',
	padding: 5,
	borderWidth: 0,
};